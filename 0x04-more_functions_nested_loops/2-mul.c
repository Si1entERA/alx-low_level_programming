#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first number
 * @b: the second number to multiply
 *
 * Return: the result of the multiplied integer between a param and b param
 */

int mul(int a, int b)
{
	return (a * b);
}
